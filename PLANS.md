# Планы

Список отложенного. Сделанное переезжает в `TROUBLESHOOTING.md` или `README.md`
с разбором, отсюда удаляется.

## Переезд на Enhanced Celestials 2

**Статус:** отложено 2026-09-19. Выигрыш косметический, ветка молодая —
логичный момент, когда дойдут руки до шейдеров.

Сейчас стоит `Enhanced-Celestials-NeoForge-1.21.1-6.0.2.6.jar` — последняя
версия старой ветки, внутри неё обновлять нечего. EC2 под 1.21.1 существует,
но это не обновление, а переезд: мод распался на три проекта.

| Модрод (slug) | Версия под 1.21.1 NeoForge | Дата |
|---|---|---|
| `enhanced-celestials-2-core` | 2.0.3.3 | 2026-09-07 |
| `enhanced-celestials-2-default-lunar-events` | 2.0.1.3 | 2026-09-03 |
| `enhanced-celestials-2-shader-support` | 2.0.0.3 | 2026-09-03 |

**Проверено перед откладыванием:**
- Ни один мод сборки не зависит от `enhancedcelestials` — прошёл по всем
  `neoforge.mods.toml`, ссылок нет. Замена ничего за собой не утянет.
- Модуль Shader Support требует Sodium и Iris — оба стоят, так что лунные
  события наконец будут видны со включёнными шейдерами (в EC1 не отображались).
- Мир не сломается: EC не добавляет блоков и предметов, только события.

**Риски:** три джарника вместо одного; Core вышел за 12 дней до этой записи;
конфиги EC1 почти наверняка не переносятся — лунные события настраивать заново.

**Порядок работ:** скачать три модуля → убрать старый джар → `preflight.py`,
`classcheck.py`, `apicheck.py` → обновить `modlist.lock.json` и `MODLIST.md`.

## Пара Sable × Copycats+

`Sable: Failed to apply tag physics properties. Unknown block:
copycats:copycat_catwalk` плюс `IllegalStateException` на
`copycats:wrapped_copycat` из mekanicalcreate — два независимых звоночка о том,
что Sable и mekanicalcreate знают не ту версию Copycats+. Сейчас оба безвредны
(ловятся и пропускаются). Если полезут графические артефакты или пропадут
рецепты копикатов — начинать отсюда.

## JEI-плагин Create: More Catalysts

Плагин падает на регистрации пустого списка категорий (совместимость с
Anvilcraft, которого в сборке нет), JEI выбрасывает его целиком. Потеряны
вкладки JEI от этого мода. Чинится либо патчем джарника, либо ожиданием
апдейта. Некритично — сам мод работает, не видно только его категорий в JEI.

## Разъезд копий скриптов проверок

`preflight.py`, `classcheck.py`, `apicheck.py` сейчас лежат **в двух местах**:
здесь, в корне сборки, и в [codex-aethernum/tools](https://github.com/codex-aethernum/tools),
где они канон. Сделано ради удобства — привычное `python preflight.py
minecraft/mods` продолжает работать без лишних телодвижений.

Копии разойдутся, если править их здесь. Варианты развязки, когда надоест:
подключить `tools` сабмодулем, или выкинуть копии отсюда и держать `tools`
склонированным рядом. Решать на момент, когда появится CI — он всё равно
будет дёргать канонические версии.

## Книга — после релиза сборки

**Решено 2026-09-19: книгу в сборку пока НЕ ставим.**

Порядок работ: сначала довести сборку до какого-то релиза — зафиксировать
состав, чтобы он перестал ехать, — и только потом обновлять
[Codex Aethernum Book](https://github.com/codex-aethernum/book) по новому составу
и ставить её в сборку. Обратный порядок означал бы переписывать задания поверх
уже установленной книги.

Книга писалась под **219** модов, сейчас в сборке **221**, и состав ещё
менялся по ходу отладки (Iris, EC2 на очереди). Расхождение нужно будет
посчитать перед обновлением заданий: `modlist.lock.json` против того, что
книга знает.

**Открытый вопрос к тому моменту: FTB Quests.** В сборке стоит весь FTB-стек —
`ftb-quests`, `ftb-library`, `ftb-teams`, `ftb-xmod-compat`. С книгой получаются
две системы заданий сразу. Конфликта нет (разные моды, разные экраны), но если
FTB Quests стоял «на будущее, под книгу», то после её установки он лишний.
Перед удалением проверить, не тянет ли FTB-стек кто-то ещё: XMod Compat
специально живёт мостом к другим модам.

**Когда дойдёт до установки:** джарник берётся из релиза книги (в релизе v0.4.0
он ещё со старым именем `codex-aeternum-0.4.0.jar` — переименование попадёт
только в следующий выпуск; `modId` внутри `codex`, имя файла ни на что не
влияет). В `client_only_mods.txt` книгу НЕ добавлять: на сервере она нужна,
иначе не будет ни прогресса, ни наград.

## Баланс и прогрессия — до книги

Главная работа по сборке и предпосылка ко всему остальному. Книга описывает
прогрессию, значит пишется **по** настроенной прогрессии: сначала решить, как
устроен путь игрока, потом закреплять его заданиями. Обратный порядок —
переписывать задания дважды.

Итоговая последовательность:

**баланс и прогрессия → релиз сборки → дельта состава → обновление книги → установка книги**

### Что уже сделано

- **Almost Unified настроен и работает.** Приоритет
  `minecraft → mekanism → immersiveengineering → create → powah → ...`
  (см. `config/almostunified/unification/materials.json`). По логу последнего
  запуска: **62 группы предметов сведены**, лишние скрыты из JEI.
- **Immersive Engineering сшит** — `kubejs/server_scripts/10_ie_stitch.js`,
  1611 строк рецептов, заменяет отключённый `engineeredcompatibility`
  (разбор — в `BRIDGES.md`).

### Что не сделано

`kubejs/server_scripts/00_expert_gating.js` — **скелет**: 32 строки, все
примеры закомментированы. Ни одного реального гейта пока нет. Заложенное
правило: ничего не удалять насовсем, а заменять более дорогим путём, чтобы
рецепт оставался видимым в JEI и логика читалась.

### С чего начинать — уже известные хвосты

Лог `logs/almostunified/almostunified.log` даёт готовый список:

- `eternal_tales:uranium` висит сразу на `#c:ingots/uranium` и `#c:gems/uranium` —
  предмет не может быть одновременно слитком и самоцветом, это чинится тегами;
- 8 рецептов `botania:marimorphosis/*` не юнифицируются;
- `aether_overworld_ores:crushing/holystone_sulfur_ore_crushing` падает
  после юнификации;
- 11 предупреждений «Could not detect crafting recipe output», почти все —
  `eternal_tales`.

### Открытые вопросы

- **Точки гейтинга между модами.** Mekanism выбран базой переработки, но
  переходы Create → IE → Mekanism → Powah пока ничем не разделены: можно
  прыгнуть в конец, минуя этапы.
- **Что делать с дублирующими путями.** В сборке 49 модов семейства Create
  (по данным mekanicalcreate) — многие дают альтернативы одному и тому же.
- **Нужен ли FTB Quests** — см. раздел про книгу выше.

## Cold Sweat: брать ли апстримную починку тега `chest_armor`

**Отложено 2026-09-19.** Менять поведение мода в только что стабилизировавшейся
сборке ради этого не стали.

### Что не так

Релизная Cold Sweat 2.4.2 кладёт три своих нагрудника

```
cold_sweat:goat_fur_chestplate, cold_sweat:hoglin_chestplate, cold_sweat:chameleon_chestplate
```

в файл `data/minecraft/tags/item/body_armor.json`. **Такого тега в 1.21.1 не
существует** — проверено по ванильному клиенту, там есть `chest_armor`,
`head_armor`, `leg_armor`, `foot_armor` и ни одного `body_armor`.

Шлем, поножи и ботинки той же брони разложены по своим тегам правильно.
Промахнулся только нагрудный слой: его предметы не попадают в
`minecraft:chest_armor`, то есть для всего, что опирается на этот тег, три
нагрудника Cold Sweat как бы не броня.

Апстрим это заметил и починил коммитом `c7918619d` **«Fix incorrectly named
"chest_armor" item tag»** от 2026-06-25 — через три дня после релиза 2.4.2.
В нашей ветке форка он сознательно не взят: цель ветки была воспроизвести то,
что реально работает в сборке.

### Что решить

Брать или нет. Аргумент за: баг настоящий и починка однострочная. Аргумент
против: это изменение поведения мода, и последствия зависят от того, кто в
сборке читает `minecraft:chest_armor` — а таких модов при 221 установленном
может оказаться немало.

### Как сделать, когда решим

```bash
cd projects/fork-coldsweat
git cherry-pick c7918619d          # поверх codex/2.4.2-aquamirae
JAVA_HOME="$APPDATA/PrismLauncher/java/java-runtime-delta" ./gradlew build
```

Дальше собранный `build/libs/ColdSweat-2.4.2.jar` кладётся в `minecraft/mods`
вместо нынешнего, и прогоняются три проверки. Заодно это станет первым реальным
применением форка — до сих пор он только воспроизводил.

**Проверять последствия так:** поискать по всем джарникам сборки, кто ссылается
на `minecraft:chest_armor` в рецептах, тегах и лут-таблицах. Если читателей нет
вовсе — починка безопасна, но и бессмысленна; если есть — станет ясно, что
именно изменится.

