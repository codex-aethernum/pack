# Гасим возню с переводом строк: в репозитории всегда LF, в рабочей копии
# Git ставит родные для системы. Без этого каждый файл конфига на Windows
# попадает в diff целиком.
* text=auto eol=lf

# Бинарное — не трогать.
*.png binary
*.ogg binary
*.jar binary
*.zip binary
